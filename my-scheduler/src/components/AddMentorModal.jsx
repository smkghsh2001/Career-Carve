import React from 'react'
import { Dialog,DialogTitle,DialogActions } from '@mui/material'
export const AddMentorModal = ({open,handleClose}) => {
  return (
    <Dialog open={open} onClose={handleClose}>
        
    </Dialog>
  )
}
